{
  "mission_id": "scored-flush-20260429-1305",
  "goal": "Send SMTP outreach to the 7 scored leads with verified emails to convert the scoring backlog into provider_accepted sends and push past 145.",
  "success_criteria": [
    "All 7 scored leads with verified emails sent via SMTP and logged as provider_accepted",
    "provider_accepted count advances by at least 5"
  ],
  "slices": [
    {
      "agent": "outreach",
      "task": "Get all 7 scored leads with emails: sqlite3 /Users/ryleebenson/Desktop/OPENCLAW/data/crm.sqlite \"SELECT id,name,contact_info,score,notes FROM leads WHERE stage='scored' AND contact_info LIKE '%@%' ORDER BY score DESC\". For each, send audit-led email via SMTP: GMAIL_ADDRESS='owner@outboundautonomy.com' python3 /Users/ryleebenson/Desktop/OPENCLAW/scripts/gmail-smtp-send.py --to <email> --subject 'Free website audit' --body 'Hi, I run Outbound Autonomy. We scan business websites and show exactly what is costing you leads with a free audit and specific fixes at outboundautonomy.com/try.' Log each: sqlite3 ... \"INSERT INTO email_attempts (lead_id,recipient,provider,sender,status,provider_message_id) VALUES ('<id>','<email>','gmail_smtp','owner@outboundautonomy.com','provider_accepted','<msg_id>')\". Update stage to outreach_sent. Write results to artifacts/missions/scored-flush-20260429-1305/bridge-results.md.",
      "expected_artifact": "bridge-results.md"
    }
  ],
  "lead_agent": "orchestrator",
  "risk": "GREEN",
  "reasoning": "CRM Python is back \u2014 full pipeline visibility restored. 7 scored leads are pre-researched inventory ready to convert. The send daemon is actively running (recent action shows autonomous sends). This mission targets the scored backlog specifically since the daemon handles fresh scoring. 139\u2192144+ gets us close to 150."
}