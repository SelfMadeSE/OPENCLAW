# Site Health & Infrastructure Report
**Date:** 2026-04-28 08:15 MDT (14:15 UTC)
**Verification: verified** (direct tool output, not inferred)
**Engineer:** FORGE

---

## PRIORITY 1: Gateway Restart Pattern

### Finding: 🔴 CONFIRMED — Recurring SIGTERM cascade

**Today's pattern (2026-04-28):**
| # | SIGTERM Time (UTC) | Notes |
|---|-------------------|-------|
| 1 | 13:28:01 | **Config-change-driven** — `gateway/reload` logged "config change detected; evaluating reload" at 13:27:28 for `agents.defaults.thinkingDefault, agents.defaults.subagents.thinking, agents.list`. Hot reload applied, then 33s later → SIGTERM. |
| 2 | 13:32:28 | 4m27s after first restart |
| 3 | 13:37:07 | 4m39s after second restart |
| 4 | 13:42:09 | **Config-change-driven** — `gateway/reload` logged at 13:42:04 for `models.providers.deepseek.models`. Hot reload applied, then **5s later** → SIGTERM. |
| 5 | 13:51:02 | **Most destructive** — killed 4 v4-pro agents mid-task (FORGE, PULSE, BRIDGE, SENTINEL v1 runs). Confirmed by gateway closed 1006 errors in log. |

**Total:** 5 SIGTERM restarts in 23 minutes (13:28–13:51 UTC).

### Root Cause: Config-change hot reload → SIGTERM

The log shows a clear causal chain:
1. A config file change is written (editing `openclaw.json`)
2. `gateway/reload` subsystem detects it: "config change detected; evaluating reload"
3. Hot reload is applied: "config hot reload applied"
4. Within seconds to minutes, SIGTERM arrives
5. Gateway restarts, killing all active agents

**At least 2 of the 5 restarts are directly linked to config changes:**
- 13:27:28 — change to `agents.defaults.thinkingDefault`, `agents.defaults.subagents.thinking`, `agents.list`
- 13:42:04 — change to `models.providers.deepseek.models`

The other 3 (13:32, 13:37, 13:51) may be macOS LaunchAgent watchdog behavior after the first config-change restart cascade. Orphaned ping/pong timing patterns from the bonjour watchdog (logged "watchdog detected non-announced service") suggest the gateway doesn't fully tear down before macOS relaunches it.

### Historical Pattern
- **Apr 18:** 49 restarts (per SENTINEL audit)
- **Apr 19:** 13 restarts
- **Apr 26:** 9 restarts
- **Apr 28 (today):** 5 in 23 min (partial day)

### Impact
- **4 v4-pro agents killed** in the 13:51 restart (FORGE, PULSE, BRIDGE, SENTINEL v1)
- All had to be retried as v2 runs, burning context/tokens
- Long-running agents (>6 min) are most vulnerable
- **Secondary finding:** 62 pending delivery entries were recovered after one restart (delivery-recovery), confirming messages were queued but not sent before the kill

### Recommendation
1. **Stop editing `openclaw.json` while agents are running** — config changes trigger hot reload which triggers SIGTERM
2. Alternatively, use `openclaw gateway stop` + `openclaw gateway start` deliberately instead of relying on hot reload
3. Run `openclaw doctor --repair` to fix the LaunchAgent PATH issue (noted in status output — nvm-managed node path can break after upgrades)

---

## PRIORITY 2: Email Infrastructure — owner@outboundautonomy.com

### Current DNS Status

| Record | Value | Status |
|--------|-------|--------|
| **MX** | `aspmx.l.google.com` (priority 1) | ✅ Set |
| | `alt1.aspmx.l.google.com` (priority 5) | ✅ Set |
| | `alt2.aspmx.l.google.com` (priority 5) | ✅ Set |
| | `alt3.aspmx.l.google.com` (priority 10) | ✅ Set |
| | `alt4.aspmx.l.google.com` (priority 10) | ✅ Set |
| **SPF** | `v=spf1 include:_spf.google.com ~all` | ✅ Set |
| **DMARC** | `v=DMARC1; p=none; rua=mailto:postmaster@outboundautonomy.com` | ✅ Set (p=none — monitoring only, no enforcement) |
| **DKIM** | `google._domainkey.outboundautonomy.com` | ❌ **No DKIM record found** |

### Assessment: PARTIALLY CONFIGURED

**What's working:**
- Domain DNS is fully **pointed at Google Workspace** (MX + SPF)
- Inbound email via Google Workspace should work
- DMARC is set to monitoring mode (`p=none`) — correct for initial setup

**What's missing:**
- **No DKIM record** — without DKIM, outbound emails are more likely to land in spam. Google Workspace generates DKIM keys automatically when enabled in the admin console.
- **No outbound provider configured** that I can see — Gateway config references `GMAIL_ADDRESS` / `GMAIL_PASSWORD` env vars but I cannot verify they have sending capability configured on the Google Workspace side.

**What's needed to send from owner@outboundautonomy.com:**
1. ✅ DNS MX pointing to Google (done)
2. ✅ SPF record (done)
3. ❌ **Enable DKIM** in Google Workspace admin → generate and publish the DKIM TXT record
4. ❌ **Upgrade DMARC** from `p=none` to `p=quarantine` after DKIM is verified working
5. ❌ **Configure SMTP/API credentials** for programmatic sending (Gmail API or SMTP relay)
6. ❌ **Set up Gmail forwarding or API access** from the current OpenClaw gateway (the "gmail-watcher" plugin is running per logs)

**⚠️ No accounts should be created, no DNS changed, and no money spent without Rylee's explicit approval.**

---

## PRIORITY 3: Live Site Verification

### All Pages Checked — 2026-04-28 14:17 UTC

| URL | Status | Content Notes |
|-----|--------|---------------|
| `https://outboundautonomy.com/` | ✅ **200** | Homepage with audit CTAs, FAQ section, full readable content |
| `https://outboundautonomy.com/services` | ✅ **200** | "Turn audit findings into working systems" — audit-led, score-band-mapped |
| `https://outboundautonomy.com/pricing` | ✅ **200** | "Three ways we work with you" — references Lane 1/2/3 (needs audit to confirm alignment) |
| `https://outboundautonomy.com/contact` | ✅ **200** | Contact form page, "Tell us which site or funnel needs attention" |
| `https://outboundautonomy.com/about` | ✅ **200** | "We find what's leaking. Then we build what fixes it." — new audit-led copy |
| `https://outboundautonomy.com/try` | ✅ **200** | Website audit preview page with Peak HVAC example audit |
| `https://outboundautonomy.com/sample-report` | ✅ **200** | Sample audit report page |
| `https://outboundautonomy.com/faq` | ❌ **404** | Returns 404 — FAQ page not deployed. FAQ content exists on the homepage but `/faq` route is missing |
| `https://outboundautonomy.com/case-studies` | ❌ **404** | Returns 404 — Case studies page not deployed |
| `https://outboundautonomy.com/privacy` | ✅ **200** | Privacy policy |
| `https://outboundautonomy.com/terms` | ✅ **200** | Terms of service |
| `https://outboundautonomy.com/cookies` | ✅ **200** | Cookie policy |
| `https://outboundautonomy.com/sitemap.xml` | ✅ **200** | **12 URLs** listed (home, services, pricing, contact, sample-report, try, case-studies, faq, about, privacy, terms, cookies) |
| `https://outboundautonomy.com/api/audit` | ❌ **405 Method Not Allowed** | API endpoint exists but rejects GET requests. POST required. Cannot test via GET. |
| `https://outboundautonomy.com/nonexistent-test-404` | ✅ **404** | Non-existent URL correctly returns 404 with styled page |

### Sitemap Analysis

**12 URLs in sitemap** (all updated 2026-04-28T14:06:58.579Z):
- `/`, `/services`, `/pricing`, `/contact`, `/sample-report`, `/try`, `/case-studies`, `/faq`, `/about`, `/privacy`, `/terms`, `/cookies`

**Mismatch:** Sitemap includes `/faq` and `/case-studies` but both return **404** when accessed. This means search engines are being told about pages that don't exist — bad for SEO.

### Audit API
- `/api/audit` rejects GET requests (405)
- Requires POST — likely expects URL in request body
- Can only verify end-to-end via a POST with a test URL, which requires knowing the expected payload format

### Key Issues Found
1. 🟠 **/faq 404** — In sitemap but no route. FAQ content on homepage but no dedicated page.
2. 🟠 **/case-studies 404** — In sitemap but no route. Sitemap claims it exists.
3. 🟡 **Pricing page** still references "Lane 1/2/3" language — may not be fully updated to audit-led approach
4. 🟢 **Sitemap lastmod** shows all URLs at same timestamp (14:06:58) — this is fine for a static site build but means search engines can't prioritize what actually changed

---

## Summary Dashboard

| Area | Status | Risk |
|------|--------|------|
| Gateway Stability | 5 SIGTERMs in 23min, config-change hot reload is the trigger | 🔴 HIGH |
| Email (owner@outboundautonomy.com) | MX + SPF + DMARC set. No DKIM. Needs admin-side enable. | 🟡 MEDIUM |
| Site — Core Pages | All 200 (home, services, pricing, about, contact, try, sample-report) | ✅ GOOD |
| Site — Missing Pages | /faq (in sitemap, 404), /case-studies (in sitemap, 404) | 🟡 MEDIUM |
| Sitemap | 12 URLs listed, but 2 return 404 | 🟡 MEDIUM |
| Audit API | Exists at /api/audit, rejects GET, needs POST | 🟢 LOW |
| Error Handling | Custom 404 page works, styled correctly | ✅ GOOD |
